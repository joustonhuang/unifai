#!/usr/bin/env bash
set -euo pipefail

STATE_FILE="/var/lib/little7/fuse_state.json"
if sudo test -f "$STATE_FILE"; then
  sudo rm -f "$STATE_FILE"
  echo "FUSE RESET: removed $STATE_FILE"
else
  echo "FUSE RESET: no state file"
fi
